{
  "version": "0.0.2",
  "name": "@ntut/permit",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "vitest watch",
    "test": "vitest run",
    "pub": "vitest run && npx jsr publish"
  },
  "description": "Easy access control tree for your application",
  "author": "ariomoklo <ariowidiatmoko@gmail.com> (ariomoklo.dev)",
  "keywords": ["rbac", "access-control", "access-control-list", "access-control-tree"],
  "devDependencies": {
    "@types/bun": "latest",
    "vite": "^5.2.11",
    "vitest": "^1.6.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}