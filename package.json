{
  "name": "@ntut/permit",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "vitest watch",
    "test": "vitest run",
    "publish": "vitest run && npx jsr publish"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vite": "^5.2.11",
    "vitest": "^1.6.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}